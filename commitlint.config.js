// release-please derives CHANGELOG.md entirely from these commit subjects, so the
// conventional-commit contract is enforced rather than assumed.
module.exports = { extends: ['@commitlint/config-conventional'] };
